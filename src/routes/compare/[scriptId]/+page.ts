// Comparison selection is encoded in ?against= and intentionally remains
// client-driven on the static deployment. adapter-static's 404 fallback
// serves direct links, while in-app navigation stays entirely client-side.
export const prerender = false;
