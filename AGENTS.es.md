# Instrucciones para agentes (español)

> **Traducción / `needs_revision`.** La fuente de política para agentes es [`AGENTS.md`](AGENTS.md) (inglés). Si este archivo diverge, prevalece el inglés.

## Objetivo

Transformar gradualmente el paquete estático de Light Delay en una aplicación SvelteKit basada en datos, sin perder canon, contenido, imágenes ni comportamiento.

## Conjunto de trabajo (vigente)

Leer `AGENTS.md` al inicio de cada sesión. Mapa corto: [`docs/AGENT_ONBOARDING.md`](docs/AGENT_ONBOARDING.md).

- **SoT narrativa:** `data/outlines/light-delay-master-narrative.json` (WIP autoritativa).
- **Producción WIP:** `script:light-delay-festival-master` (+ escaleta derivada). Tráiler: `script:light-delay-trailer-master`.
- El stub maestro / `canonicalScriptId` no implica ausencia de guion de producción.
- No confundir `light-delay-festival` (deprecado) con `light-delay-festival-master`.
- Master incompleto bloquea *nuevos* cuts no autorizados; ver `data/editorial-lifecycle.json` para derivados WIP ya autorizados.
- Autoría en inglés primero; español puede quedar `needs_revision`.

## Capas (prevalece lo superior) y compuerta hacia arriba

Ver el diagrama y la **compuerta de propagación hacia arriba (dos aprobaciones)** en `AGENTS.md`. No “corregir” en silencio una capa inferior contra la verdad de la escaleta/master.

## Idioma y reglas

La política completa (idioma, reglas obligatorias, arquitectura, validación — incluido el animatic deprecado **128** tomas) está en `AGENTS.md`. Este archivo no debe duplicarla; actualizarlo en un pase de traducción posterior.
